{
  "name": "@clairtus/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build:deno": "esbuild src/index.ts --bundle --format=esm --platform=neutral --outfile=dist/core.deno.js"
  },
  "dependencies": {
    "@clairtus/shared": "workspace:*",
    "@clairtus/ledger": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "@electric-sql/pglite": "^0.2.17"
  }
}
